{
  "name": "observability-agent",
  "version": "0.1.0",
  "private": true,
  "description": "OA (Observability Agent) - in-cluster read-only gateway for logs, events, and pod metrics scraping.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.3.0",
    "fastify": "^5.2.1",
    "jsonwebtoken": "^9.0.2",
    "undici": "^7.4.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.8",
    "@types/node": "^22.13.1",
    "@vitest/coverage-v8": "^4.0.18",
    "typescript": "^5.7.3",
    "vitest": "^4.0.18"
  }
}
